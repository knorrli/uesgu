<%# locals: (id:, title:, merged: false) %>
<p id="<%= id %>" class="capture-card__status capture-card__status--published">
  <%= merged ? t('capture.card.merged', title: title) : t('capture.card.published', title: title) %>
</p>
