<% content_for :title, t('events.index.title') %>

<div class="flex flex-col gap-large align-stretch" data-controller="filter favorite"
     data-favorite-followed-value="<%= favorite_followed_keys.to_json %>">
  <%= render 'filter', filter: @filter %>

  <%# Toggle row, above the content in both views (so the first bold header lines
      up across views). When the filter comes up empty, the message rides on the
      left of this same always-present row, so nothing below it jumps. %>
  <div class="flex justify-between align-baseline gap-medium">
    <span class="muted"><%= t('events.index.empty') if @events.empty? %></span>
    <%= render 'view_switcher', view: @view %>
  </div>

  <% if @view == 'calendar' %>
    <%= render 'calendar', events: @events %>
  <% elsif @events.any? %>
    <%= render 'events', events: @events %>
  <% end %>
</div>

