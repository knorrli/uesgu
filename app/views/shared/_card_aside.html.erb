<%# locals: (title:, action: nil) %>
<aside class="card-aside">
  <h2 class="card-aside__title"><%= title %></h2>
  <%= yield %>
  <%= tag.div(action, class: "card-aside__actions") if action.present? %>
</aside>
