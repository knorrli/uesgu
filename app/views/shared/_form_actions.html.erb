<%# locals: (form:, submit_label:, cancel_path:) %>
<div class="form-actions">
  <%= form.submit submit_label, class: 'button-small primary' %>
  <%= link_to t('shared.cancel'), cancel_path, class: 'button-small' %>
  <%= yield if block_given? %>
</div>
