# See https://www.robotstxt.org/robotstxt.html for documentation on how to use the robots.txt file
#
# robots.txt is read only by automated crawlers that choose to obey it — real
# users' browsers never fetch it, so nothing here can affect a human visitor.
#
# üsgu is invite-only: there is no audience for it in a search index, and nothing
# here is meant to be discovered from outside. So the answer to every crawler is
# simply "no" — not the previous surgical /*? carve-out, which tried to protect
# only the faceted long tail and was ignored outright by the crawler that burned
# 10GB of bandwidth in August 2026 (100% of its requests hit Disallow: /*? URLs).
#
# This file is now the POLITE half of the policy. The half that actually holds is
# enforcement, because the crawlers that cost money are exactly the ones that
# don't read this: a sitewide X-Robots-Tag: noindex, nofollow (see
# ApplicationController) plus User-Agent blocking and per-IP + global throttling
# in config/initializers/rack_attack.rb.

User-agent: *
Disallow: /
